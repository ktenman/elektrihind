image: $CI_REGISTRY_IMAGE/maven-jdk-21:3.9.5

stages:
  - build
  - test

cache:
  paths:
    - .m2/repository

build:
  stage: build
  script:
    - mvn compile

test:
  stage: test
  script:
    - mvn test
