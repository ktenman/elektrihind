image: docker:24.0.7

services:
  - name: docker:24.0.7-dind
    alias: docker

variables:
  DOCKER_DRIVER: overlay2
  IMAGE_TAG: $CI_REGISTRY_IMAGE/maven-jdk-21:latest
  # DOCKER_HOST is not needed if using the default unix socket
  DOCKER_TLS_CERTDIR: ""  # Disabling TLS for simplicity, be aware of security implications in a production environment

stages:
  - diagnostics
  - build_image
  - build
  - test

diagnostics:
  stage: diagnostics
  script:
    - echo "Checking Docker version..."
    - docker --version || true
    - echo "Checking which Docker..."
    - which docker || true
    - echo "Checking PATH..."
    - echo $PATH
    - echo "Checking user permissions..."
    - id
    - echo "Checking if Docker service is up..."
    - ps aux | grep docker || true

before_script:
  - echo "Waiting for Docker to start..."
  - sleep 10
  # Logging in securely using password stdin
  - echo "Logging into GitLab Registry..."
  - echo $CI_REGISTRY_PASSWORD | docker login -u $CI_REGISTRY_USER --password-stdin $CI_REGISTRY

build_image:
  stage: build_image
  script:
    - echo "Building the Docker image..."
    - docker build --pull -t "$IMAGE_TAG" .
    - echo "Pushing the image to the registry..."
    - docker push "$IMAGE_TAG"

cache:
  key: "$CI_COMMIT_REF_NAME"
  paths:
    - .m2/repository

build:
  stage: build
  image: "$IMAGE_TAG"
  services: []
  before_script:
    - echo "Installing Docker client..."
    - apt-get update -y && apt-get install -y docker.io
    - echo "Logging into GitLab Registry within the build image..."
    # Again, using password stdin for login
    - echo $CI_REGISTRY_PASSWORD | docker login -u $CI_REGISTRY_USER --password-stdin $CI_REGISTRY
  script:
    - echo "Compiling the project..."
    - mvn compile
    - echo "Listing contents of the Maven repository to ensure it's populated..."
    - ls -la ~/.m2/repository/ || true

test:
  stage: test
  image: "$IMAGE_TAG"
  services: []
  before_script:
    - echo "Installing Docker client..."
    - apt-get update -y && apt-get install -y docker.io
    - echo "Logging into GitLab Registry within the build image..."
    # Again, using password stdin for login
    - echo $CI_REGISTRY_PASSWORD | docker login -u $CI_REGISTRY_USER --password-stdin $CI_REGISTRY
  script:
    - echo "Running tests..."
    - mvn test
