package ee.tenman.elektrihind.apollo;

public enum ApolloKinoState {
    INITIAL,
    SELECT_MOVIE,
    SELECT_DATE,
    SELECT_TIME,
    SELECT_ROW,
    SELECT_SEAT,
    CONFIRMATION,
    COMPLETED
}

