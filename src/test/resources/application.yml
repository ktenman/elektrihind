telegram:
  botToken:
  monitoringChatId:
  privateChatId:
  elektriteemu:
    token:
    username:

electricity-prices-api:
  url: http://localhost:${wiremock.server.port}

omdb-api:
  url: http://localhost:${wiremock.server.port}
  key:

captcha-solver:
  url: http://localhost:55224

twocaptcha:
  key: 97dc669cc0e30cd46c2d2e9a0975bb2e

scrapeninja:
  key:

imdb-api:
  url: http://localhost:${wiremock.server.port}

digitalocean:
  token:

spring:
  data:
    redis:
      host: localhost
      port: 6379
      password: something

app.messaging.type: rabbitmq

vision:
  base64EncodedKey:

openai:
  token:
