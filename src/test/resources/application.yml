telegram:
  botToken:
  monitoringChatId:
  privateChatId:
  elektriteemu:
    token:
    username:

electricity-prices-api:
  url: http://localhost:${wiremock.server.port}

omdb-api:
  url: http://localhost:${wiremock.server.port}
  key:

twocaptcha:
  key:

imdb-api:
  url: http://localhost:${wiremock.server.port}

digitalocean:
  api:
    token:
