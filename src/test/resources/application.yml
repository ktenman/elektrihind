telegram:
  botToken:
  monitoringChatId:
  privateChatId:
  elektriteemu:
    token:
    username:

electricity-prices-api:
  url: http://localhost:${wiremock.server.port}

omdb-api:
  url: http://localhost:${wiremock.server.port}
  key:

twocaptcha:
  key:

scrapeninja:
  key:

imdb-api:
  url: http://localhost:${wiremock.server.port}

digitalocean:
  api:
    token:

spring:
  data:
    redis:
      host: localhost
      port: 6379

vision:
  base64EncodedKey:
