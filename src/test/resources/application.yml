telegram:
  botToken:
  monitoringChatId:
  privateChatId:

electricity-prices-api:
  url: http://localhost:${wiremock.server.port}

omdb-api:
  url: http://localhost:${wiremock.server.port}
  key:

imdb-api:
  url: http://localhost:${wiremock.server.port}
