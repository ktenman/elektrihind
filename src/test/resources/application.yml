telegram:
  botToken:
  monitoringChatId:
  privateChatId:
  elektriteemu:
    token:
    username:

electricity-prices-api:
  url: http://localhost:${wiremock.server.port}

omdb-api:
  url: http://localhost:${wiremock.server.port}
  key:

captcha-solver:
  url: http://localhost:55224

automaks:
  url: http://localhost:52526

twocaptcha:
  key: e3a4665821cb079d4c6b1fc7f60fcbaa

scrapeninja:
  key:

imdb-api:
  url: http://localhost:${wiremock.server.port}

digitalocean:
  token:

spring:
  data:
    redis:
      host: localhost
      port: 6379
      password: something

app.messaging.type: rabbitmq

vision:
  base64EncodedKey:

openai:
  token:

apollo-kino:
  username:
  password:
  username-password-pairs:
